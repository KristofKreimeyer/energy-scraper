{
  "name": "energy-scraper-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker: Bestpreis-Alarm – Anmeldung (Double-Opt-In) + Abo-Speicher in D1. Kanal E-Mail zuerst.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply findmyenergy --local",
    "db:migrate": "wrangler d1 migrations apply findmyenergy --remote"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "~5.7.2",
    "wrangler": "^3.90.0"
  }
}
